import React from 'react';
import GrandParents from '../../GrandParents';

const PropsDilling = () => {
    return (
        <div>
            <GrandParents></GrandParents>
        </div>
    );
};

export default PropsDilling;